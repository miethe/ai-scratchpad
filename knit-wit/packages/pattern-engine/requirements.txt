# Core dependencies for knit-wit-engine
# For modern usage, prefer: uv pip install -e .

pydantic>=2.0.0,<3.0.0
numpy>=1.24.0,<2.0.0

# Development dependencies
# Install with: uv pip install -e ".[dev]"
# Or: pip install -e ".[dev]"
